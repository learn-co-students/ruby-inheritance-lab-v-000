#!/usr/bin/env ruby

require_relative "../lib/user.rb"
require_relative "../lib/teacher.rb"
require_relative "../lib/student.rb"

# steve = Student.new
# steve.first_name = "Steve"
# steve.last_name = "Jobs"

vladmir = Student.new
vladmir.first_name = "Vladmir"
vladmir.last_name = "Putin"


# jim = User.new
# jim.first_name = "Jim"
# jim.last_name = "Bean"

avi = Teacher.new
avi.first_name = "Avi"
avi.last_name = "Flombaum"


some_knowledge = avi.teach

#steve.learn(some_knowledge)

vladmir.learn(some_knowledge)

#jim.learn(some_knowledge)

puts "Vladmir just learned this important knowledge: '#{vladmir.knowledge.first}' from Avi"
